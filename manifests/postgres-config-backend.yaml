apiVersion: v1
data:
  rds-host-name: "otus-postgresql.postgres.svc.cluster.local"
  rds-port: "5432"
  rds-db-profile-name: "users"
  rds-db-auth-name: "credentials"
  rds-user-name: "otus"
kind: ConfigMap
metadata:
  name: otus-app-config
  namespace: backend